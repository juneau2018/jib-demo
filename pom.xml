<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.mycompany.app</groupId>
  <artifactId>my-app</artifactId>
  <packaging>jar</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>my-app</name>
  <url>http://maven.apache.org</url>
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.1</version>
        <configuration>
	        <source>8</source>
	        <target>8</target>
        </configuration>
      </plugin>      
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>2.4.0</version>
        <configuration>
          <allowInsecureRegistries>true</allowInsecureRegistries>
          <extraDirectory>${project.basedir}/src/main/jib</extraDirectory>
          <from>
            <image>registry.example.com/library/openjdk:8-alpine</image>
            <auth>
              <username>${env.REGISTRY_USERNAME}</username>
              <password>${env.REGISTRY_PASSWORD}</password>
            </auth>
          </from>
          <to>
            <image>registry.example.com/common/jib-demo</image>
            <auth>
              <username>${env.REGISTRY_USERNAME}</username>
              <password>${env.REGISTRY_PASSWORD}</password>
            </auth>
            <tags>
              <tag>built-with-jib</tag>
              <tag>latest</tag>
            </tags>
          </to>
          <container>
            <!--
            <useCurrentTimestamp>true</useCurrentTimestamp>
            -->
            <creationTime>USE_CURRENT_TIMESTAMP</creationTime>
            <appRoot>/app</appRoot>
            <environment>
              <JAVA_OPTS>-Xms512m -Xdebug</JAVA_OPTS>
            </environment>
            <entrypoint>
              <arg>/bin/sh</arg>
              <arg>-c</arg>
              <arg>java ${JAVA_OPTS} -cp /app/resources/:/app/classes/:/app/libs/* com.mycompany.app.App</arg>
            </entrypoint>
            <ports>
              <port>8080</port>
              <port>2000-2003/tcp</port>
            </ports>
            <labels>
              <key1>value1</key1>
              <key2>value2</key2>
            </labels>
          </container>
        </configuration>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>build</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
